apiVersion: v1
kind: Secret
metadata:
  name: {{ app_name }}-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  url: {{ repo_url }}
  password: {{ repo_token }}
  username: {{ repo_username }}
